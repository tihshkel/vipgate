# 🚀 Инструкция по разработке фронтенда

## Быстрый старт

### 1. Установка зависимостей
```bash
cd Frontend
npm install
```

### 2. Создание файла конфигурации

Создайте файл `.env` в папке `Frontend` со следующим содержимым:

```env
# Использовать моки API вместо реального бэкенда
# true - использовать заглушки (работа БЕЗ бэкенда) ✅ РЕКОМЕНДУЕТСЯ для разработки
# false - использовать реальный API (нужен запущенный бэкенд)
VITE_USE_MOCK_API=true

# URL бэкенда (используется только если VITE_USE_MOCK_API=false)
VITE_API_BASE_URL=http://localhost:8000
```

### 3. Запуск проекта

```bash
npm run dev
```

Откройте `http://localhost:5173` в браузере.

---

## 🎯 Режимы работы

### Режим 1: С моками (без бэкенда) ✅ РЕКОМЕНДУЕТСЯ

**Настройка:**
```env
VITE_USE_MOCK_API=true
```

**Преимущества:**
- ✅ Можно разрабатывать фронтенд независимо от бэкенда
- ✅ Не нужно запускать Django сервер
- ✅ Коды верификации выводятся в консоль браузера
- ✅ Быстрое тестирование UI

**Как использовать:**
1. При отправке кода на email, код будет выведен в консоль браузера
2. Откройте DevTools (F12) → вкладка Console
3. Там вы увидите: `🔐 КОД ВЕРИФИКАЦИИ (МОК):` и сам код
4. Используйте этот код для входа

**Пример вывода в консоли:**
```
🔐 КОД ВЕРИФИКАЦИИ (МОК):
Email: user@example.com
Код: 123456
Этот код можно использовать для входа
```

---

### Режим 2: С реальным бэкендом

**Настройка:**
```env
VITE_USE_MOCK_API=false
VITE_API_BASE_URL=http://localhost:8000
```

**Требования:**
- ✅ Должен быть запущен Django бэкенд на `http://localhost:8000`
- ✅ Все API эндпоинты должны быть доступны

**Как запустить бэкенд:**
```bash
cd Backend/vipgate
python manage.py runserver
```

---

## 🔧 Что можно разрабатывать БЕЗ бэкенда

С режимом моков (`VITE_USE_MOCK_API=true`) можно полностью разрабатывать:

- ✅ Все страницы и компоненты
- ✅ Формы авторизации (коды в консоли)
- ✅ Личный кабинет
- ✅ Страницы бронирования
- ✅ Навигация и роутинг
- ✅ UI/UX компоненты
- ✅ Адаптивная верстка
- ✅ Все стили и анимации

**Единственное ограничение:**
- Коды верификации не отправляются на реальный email, а выводятся в консоль
- Но это даже удобнее для разработки! 🎉

---

## 📝 Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера с hot-reload |
| `npm run build` | Создание production сборки |
| `npm run preview` | Предпросмотр production сборки |

---

## 🐛 Решение проблем

### Проблема: API запросы не работают

**Решение:**
1. Проверьте, что файл `.env` создан в папке `Frontend`
2. Убедитесь, что `VITE_USE_MOCK_API=true` установлен
3. Перезапустите dev-сервер (`npm run dev`)

### Проблема: Коды не появляются в консоли

**Решение:**
1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Убедитесь, что консоль не фильтруется (показываются все сообщения)
4. Попробуйте отправить код заново

### Проблема: Хочу использовать реальный API

**Решение:**
1. Измените в `.env`: `VITE_USE_MOCK_API=false`
2. Убедитесь, что бэкенд запущен на `http://localhost:8000`
3. Перезапустите dev-сервер

---

## 📚 Структура API моков

Моки находятся в `src/utils/mockApi.js`:

- `mockSendCode(email)` - отправка кода верификации
- `mockVerifyCode(email, code)` - проверка кода

Все моки имитируют реальное поведение API:
- ✅ Проверка срока действия кода (10 минут)
- ✅ Ограничение попыток (5 попыток)
- ✅ Имитация задержки сети
- ✅ Правильные ответы и ошибки

---

## 💡 Советы

1. **Всегда используйте режим моков для разработки UI** - это быстрее и удобнее
2. **Проверяйте консоль браузера** - там будут коды верификации
3. **Меняйте режимы легко** - просто измените `.env` и перезапустите сервер
4. **Большую часть функционала можно разработать без бэкенда** - только UI/UX работы не требуют API

---

**Удачи в разработке! 🚀**





